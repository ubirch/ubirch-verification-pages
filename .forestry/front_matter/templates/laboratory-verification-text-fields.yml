---
label: laboratory-verification-text-fields
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: title + initial filename (for forestry overview - not visible in UI)
  description: When creating a new page the filename is created from this title; the
    translated file is found via the filename; so ensure to use the same title during
    creation of new pages for textfiles of the same content for different languages
    and check that a page with the same title doesn't exist already. (when struggling
    check filenames on GitHub)
- name: company_slogan
  type: text
  config:
    required: false
  label: company slogan
  description: optional - if your company has a slogan
- name: verification_summary
  type: text
  config:
    required: false
  label: Summary of Description of Verification
- name: verification_description_long
  type: text
  config:
    required: false
  label: Long Description of Verification
- name: layout
  type: text
  config:
    required: false
  label: layout
  default: coronalaboratory
  hidden: true
  description: Layout of Corona laboratory verification pages will always be "coronalaboratory"
pages:
- content/en/v/gov-digital.md
